[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: mark test as async
    integration: mark test as requiring external infrastructure
filterwarnings =
    ignore::DeprecationWarning
addopts =
    --ignore=tests/test_dashboard_app_clean.py
    --ignore=tests/test_dashboard_welcome_health.py
