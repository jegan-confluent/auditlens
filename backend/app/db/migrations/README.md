# Backend Migrations

The backend foundation currently uses SQLAlchemy metadata creation for the initial
`audit_events` table. Add Alembic migrations here before the schema is promoted
to a production compatibility contract.
