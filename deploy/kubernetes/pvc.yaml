apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auditlens-data
  namespace: auditlens
  labels:
    app.kubernetes.io/name: auditlens
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
